# Vendored three.js

`three.module.js` and `three.core.js` are the unmodified ES-module build of
[three.js](https://github.com/mrdoob/three.js) r186 (npm package `three@0.186.0`),
copied here so the application runs with zero network requests. License: MIT (see LICENSE).
